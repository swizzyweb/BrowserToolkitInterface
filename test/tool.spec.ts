//import { SwizzyTool, SwizzyToolProps } from "../src";
//import { description, expectedOutputDescription } from "./test-data";
describe("doSomething", () => {
  beforeAll(() => {
    // Runs once before all tests
  });

  beforeEach(() => {
    // Runs before each test
  });

  afterEach(() => {
    jest.clearAllMocks(); // Clear mocks after each test
  });

  afterAll(() => {
    // Runs once after all tests
  });

  it("JSON.stringify(Description) should contain all properties", () => {
    //    const tool = new MyTool({});
    //    const described = tool.describe();
    //    expect(described).toEqual(expectedOutputDescription);
  });
});
/*
interface MyToolProps {}

class MyTool extends SwizzyTool {
  constructor(props: MyToolProps) {
    super({
      name: "MyTool",
      description,
    });
  }
}*/
